function greaterNumber(a, b) {
  if (a > b) {
    console.log(a);
  } else {
    console.log(b);
  }

  // ctrl + /
  // let maxNumber = Math.max(a, b);
  // console.log(maxNumber);
}
greaterNumber("-5", "5");
